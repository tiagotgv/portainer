angular.module('portainer.kubernetes', [])
.controller('KubernetesDashboardController', ['$scope', 'Notifications',
function ($scope, Notifications) {

  function initView() {

  }

  initView();
}]);
